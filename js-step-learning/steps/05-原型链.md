# Step 05 原型链

目标：理解对象属性是如何沿原型链查找的。

## 关键点
- 对象访问属性时先看自己。
- 自身没有就沿 `Object.getPrototypeOf(obj)` 向上找。
- 顶端是 `null`。

## 练习
1. 创建构造函数 `User`，把 `say` 放到 `User.prototype`。
2. 用 `Object.getPrototypeOf` 打印链路。

## 通关标准
- 你能解释“属性命中对象本身”和“命中原型”的区别。
