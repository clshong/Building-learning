# Step 14 属性描述符与对象冻结

目标：掌握对象属性可写性、可枚举性、可配置性。

## 关键点
- `Object.defineProperty` 控制：`writable/enumerable/configurable`
- `Object.preventExtensions`：不可扩展
- `Object.seal`：不可扩展 + 属性不可配置
- `Object.freeze`：最严格（浅冻结）

## 通关标准
- 你能用描述符限制对象字段修改。
- 你能解释 `freeze` 是“浅冻结”。
