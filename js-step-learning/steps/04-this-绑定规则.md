# Step 04 this 绑定规则

目标：掌握 `this` 的 4 条核心规则和优先级。

## 规则优先级
1. `new` 绑定
2. 显式绑定（`call/apply/bind`）
3. 隐式绑定（`obj.fn()`）
4. 默认绑定（严格模式 `undefined`）

箭头函数没有自己的 `this`，取外层词法 `this`。

## 练习
1. 在练习文件里补全每个 case 的输出。
2. 比较普通函数和箭头函数在对象方法中的差异。

## 通关标准
- 你能按优先级解释任何 `this` 结果。
