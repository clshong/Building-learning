# Step 15 事件循环（Event Loop）

目标：搞清楚同步、微任务、宏任务执行顺序。

## 关键点
- 同步代码先执行
- 每轮事件循环结束前，先清空微任务队列
- 再进入下一个宏任务（如 `setTimeout`）

## 常见微任务
- `Promise.then/catch/finally`
- `queueMicrotask`

## 常见宏任务
- `setTimeout/setInterval`
- I/O、UI 事件

## 通关标准
- 你能稳定写出一段混合任务代码的输出顺序。
