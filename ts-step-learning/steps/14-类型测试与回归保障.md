# Step 14 类型测试与回归保障

## 原理理解
- 类型系统也需要回归测试，避免重构后类型退化。
- 可使用 `Expect` / `Equal` 辅助类型及 `@ts-expect-error`。

## 练习
1. 为 2 个工具类型补充类型断言用例。
2. 增加 1 个 `@ts-expect-error` 用例验证错误路径。

## 通关标准
- 能把关键类型能力纳入自动化回归。
