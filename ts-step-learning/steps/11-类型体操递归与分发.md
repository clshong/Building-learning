# Step 11 类型体操：递归与分发

## 原理理解
- 条件类型在联合类型上会发生分发。
- 递归类型可用于深层对象变换（如 `DeepReadonly<T>`）。

## 练习
1. 手写 `DeepReadonly<T>`，覆盖对象和数组。
2. 写一个分发条件类型，把 `A | B` 转成 `A[] | B[]`。

## 通关标准
- 能解释“为什么会分发”，并能写出递归类型工具。
