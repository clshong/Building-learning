# Step 03 State 与批处理更新

## 原理理解
- `setState` / `setX` 会触发重新渲染，不是同步立即改变量。
- React 会在合适时机做批处理，减少不必要渲染。

## 练习
1. 编写一个计数器，分别演示“直接赋值式更新”和“函数式更新”差异。
2. 解释为什么连续调用多次更新时推荐函数式写法。

## 通关标准
- 能正确解释“状态快照”和批处理带来的现象。
